<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/play_way_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="@drawable/bg_gray_round_corner_2dp"
        android:gravity="center"
        android:paddingBottom="5dp"
        android:paddingTop="5dp"
        android:text="五星直选"
        android:textColor="@color/text_color_title"
        android:textSize="@dimen/text_size_12sp" />

    <ImageView
        android:id="@+id/isJiajiang"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:src="@drawable/ic_play_way_jiajiang" />

    <ImageView
        android:id="@+id/isChoosed"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/play_way_name"
        android:layout_alignParentRight="true"
        android:src="@drawable/ic_play_way_choosed" />
</RelativeLayout>